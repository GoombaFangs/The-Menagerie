#include "menu_option.h"

const char* menu_list[] =
{
    "   Map   ",
    "   Zoo   ",
    "Inventory",
    "  Exit   ",
    "  Doag   "
};

const int menu_num = sizeof(menu_list) / sizeof(menu_list[0]);